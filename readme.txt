Vercel.json
// This file is used to configure the Vercel deployment for the Python API.
// It specifies the build and routing configuration for the API.
// The API is built using the @vercel/python builder, and all requests are routed to the index.py file.  